using System;
using System.Collections.Generic;
using System.Text;

namespace DPObserver.NetApi
{
    interface Interface
    {
        /*
        public interface System.IObserver<T> == DPObserver.IObserver<T>
        public interface System.IObservable<T> == Subject<T>
        */
    }
}
